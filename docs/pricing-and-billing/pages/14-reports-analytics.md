你现在正在一个基于 **Ant Design Pro（React + TypeScript + Ant Design）** 的后台管理系统中开发一个新的业务模块。

## 一、业务背景

这是一个 **Wholesale Banking Pricing & Billing System（批发银行定价与计费系统）** 的 Demo。

系统面向银行内部用户，用于管理企业客户定价、计费和发票流程。

当前需要实现的是其中的：

> **Analytics & Reporting（分析报表）**

重点展示平台如何提供多维度业务报表、快速预览与导出能力，支持分行与产品团队的运营分析和规划决策。

本次 Demo 暂时**不接真实后端，全部使用 Mock 数据**。

---

## 二、本次需要实现的功能

请新增或完善一个：

> **Analytics & Reporting（分析报表）**

页面。

需要体现以下业务概念：

* Quick Reports（快速报表入口）
* Report Builder（报表构建器）
* Multi-dimensional Analytics（按市场/产品/客群维度分析）
* Preview Before Download（预览后导出）
* Report History（报表历史）
* Capacity Planning Insights（产能规划参考）

---

## 三、菜单和路由

请在现有菜单中确认并使用：

```text
Reports
  └── Analytics & Reporting
```

建议路由：

```text
/pricing-billing/reports/analytics
```

---

## 四、页面总体结构

页面采用：

> ProCard + ProForm + ProTable + ProList

布局。

整体示意：

```text
┌─────────────────────────────────────────────────────────────────┐
│ Analytics & Reporting                                            │
├──────────────────────────────────────────────────────────────────┤
│ Quick Report Cards                                               │
├──────────────────────────────────────────────────────────────────┤
│ Report Builder (type/period/market/product/groupBy/format)      │
│ [Generate] [Download]                                            │
├──────────────────────────────────────────────────────────────────┤
│ Report Preview Table                                             │
├──────────────────────────────────────────────────────────────────┤
│ Recent Reports                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 五、Mock 数据

至少包含以下报表类型：

* Revenue Summary
* Pricing Execution
* Invoice Summary
* Deal Performance
* Tax Report
* Capacity Planning

并覆盖 APAC 市场（SG/HK/CN/JP/AU）的演示数据。

---

## 六、列表字段

预览表（以 Revenue Summary 为例）至少包含：

1. Market
2. Product
3. Segment (optional)
4. Client Count
5. Volume
6. Revenue
7. Currency
8. YoY Change
9. MoM Change (optional)
10. Notes (optional)

Recent Reports 至少包含：

1. Report ID
2. Report Type
3. Period
4. Market Scope
5. Format
6. Generated By
7. Generated At
8. Status
9. Actions

Actions：

```text
Preview
Download
Re-run
```

---

## 七、新增 / 编辑 Report Request

通过 Report Builder 配置请求参数，建议分组：

### 1. Report Scope

```text
Report Type
Period
Market
Product
Segment
```

### 2. Aggregation & Format

```text
Group By
Currency
Format (Excel / PDF / CSV)
```

### 3. Output

```text
Generate
Download
Save Template (optional)
```

---

## 八、Report Detail / Preview

点击 Preview 展示报表详情，建议使用：

> ProTable + ProDescriptions

展示：

```text
Report Metadata
Dimension Breakdown
Key Indicators
Generated Time
```

并增加：

### Insight Summary Preview

```text
Top Market Contributor
Top Product Contributor
Largest YoY Increase
Potential Capacity Signal
```

---

## 九、页面顶部增加区域概览

建议统计卡：

```text
Reports Generated (MTD)
Most Used Report Type
Average Generation Time
Download Count (MTD)
```

可补充：

```text
Open Report Templates
```

---

## 十、与 Pricing / Billing / Performance 的业务关系

页面中需体现：

> Analytics 汇总 Pricing、Billing、Performance 数据，为管理层和分行用户提供统一分析入口。

流程建议：

```text
Pricing & Billing Data
   ↓
Report Builder
   ↓
Preview & Insight
   ↓
Download & Distribution
   ↓
Planning / Governance Action
```

---

## 十一、Mock 数据与 API

建议结构：

```ts
interface ReportRequest {
  reportType: 'REVENUE_SUMMARY' | 'PRICING_EXECUTION' | 'INVOICE_SUMMARY' | 'DEAL_PERFORMANCE' | 'TAX_REPORT' | 'CAPACITY_PLANNING';
  period: string;
  market?: string;
  product?: string;
  groupBy?: 'MARKET' | 'PRODUCT' | 'SEGMENT' | 'RM';
  format: 'EXCEL' | 'PDF' | 'CSV';
}
```

Mock API：

```text
POST   /api/reports/generate
GET    /api/reports/preview
GET    /api/reports/:id/download
GET    /api/reports/history
```

---

## 十二、技术要求

必须遵循当前项目已有技术栈：

* React
* TypeScript
* Ant Design
* Ant Design Pro
* ProForm
* ProTable
* ProCard
* ProList

不要引入新的 UI framework。不要升级依赖。

---

## 十三、交互要求

至少实现：

### 查询/筛选

```text
Report Type
Period
Market
Product
Group By
Format
```

### 生成报表

Generate → Mock 返回 READY 报表记录。

### 预览报表

Preview → 显示表格数据。

### 下载报表

Download → 触发 Mock 文件下载。

### 历史记录

Recent Reports 展示最近生成记录并支持再次下载。

---

## 十四、Demo 重点

这个页面不是为了展示“导出按钮”，而是为了向银行客户展示：

> **集中式分析报表能力，可支撑跨国家、跨产品的经营分析与治理。**

建议突出：

```text
Unified Data Perspective
        ↓
Flexible Report Building
        ↓
Actionable Insights
        ↓
Decision Support
```

---

## 国际化要求（仅中文 / English）

- 本页面仅支持 `zh-CN` 和 `en-US` 两种语言，不新增或要求其他语言包。
- 所有 UI 文案，包括报表标题、指标、图表图例、筛选器、表格列、状态、按钮、导出和错误提示，必须使用 i18n key，不得硬编码中文或英文。
- 新增文案统一维护在 `src/locales/zh-CN/pages.ts`、`src/locales/en-US/pages.ts` 和对应的 `menu.ts` 中，使用 `pages.reports.analytics.*` 与菜单专属 key。
- 市场、币种、产品和 Mock 数据值可以保留标准值；图表维度、聚合方式和状态的展示名称必须国际化。
- 切换 `zh-CN` / `en-US` 后，报表筛选、图表、数据表、详情和导出提示都必须显示对应语言。

## 十五、实现要求

在开始修改代码之前：

1. 先检查当前项目目录结构。
2. 检查现有 routes 配置方式。
3. 检查现有菜单配置方式。
4. 检查现有报表/表格组件风格。
5. 检查现有 Mock 数据组织方式。
6. 尽可能复用已有组件和代码模式。

然后实现：

* 页面
* 路由
* 菜单
* Mock 数据
* 报表构建器
* 预览
* 下载
* 历史记录
* Insight Summary Preview

完成后确保 TypeScript 编译没有明显错误，页面能够正常运行。

**不要实现真实数仓、真实 ETL、真实报表服务后端。当前目标是可用于客户演示的高质量 Demo。**
